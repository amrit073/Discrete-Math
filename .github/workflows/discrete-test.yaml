name: C/C++ tests


on: 
  push:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: compile
        run: gcc ./*/*.c -o out

